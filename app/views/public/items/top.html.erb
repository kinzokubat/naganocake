<div class="container">
    <div class='row'>
        <div class='col-xs-3'>
            <div style="margin-top: 30px;" >
            <h2>ジャンル検索</h2>
            <table class='table'>
                <% @genres.each do |genre| %>
                  <tr>
                    <th><%= genre.name %></th>
                  </tr>
                <% end %>
            </table>
        </div>
        </div>
    <div style="margin-top:40px;" >
        <div class='col-xs-9'>
              <p>
               ようこそ、NaganoCakeへ！<br>
               このサイトはケーキ販売のECサイトになります。<br>
               会員でない方も商品の閲覧は出来ますが、<br>
               購入には会員登録が必要になります。<br>
              </p>

            <h2>オススメ商品</h2>
            <table class='table'>
                <% @items.each do |item| %>
                    <tr><%= link_to item.name, item_path(item) %></tr>
                    <p><%= (item.price * 1.1.round(2)).round %>円</p>
                <% end %>
            </table>
            <div class="pull-right">
                <%= link_to "もっと見る>", items_path , class: "btn btn-info active"%>
            </div>
        </div>
    </div>
</div>