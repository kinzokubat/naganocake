<div class="container">
    <div class='row'>
    
    <div style="margin-top:40px;" >
        <div class='col-xs-12'>
              <p>
               ようこそ、NaganoCakeへ！<br>
               このサイトはケーキ販売のECサイトになります。<br>
               会員でない方も商品の閲覧は出来ますが、<br>
               購入には会員登録が必要になります。<br>
              </p>

            <h2>オススメ商品</h2
            
            
                    <!--ジャンル部分-->
                <div class="dropdown">
                	<button class="btn btn-default dropdown-toggle" type="button" data-toggle="dropdown" aria-expanded="false">
                		ジャンル検索
                		<span class="caret"></span>
                	</button>
                	<ul class="dropdown-menu" role="menu">
                	  <% @genres.each do |genre| %>
                    <li role="presentation"><%= link_to genre.name, items_path(genre: genre.id) %></li>
                    <% end %>
                	</ul>
                </div><br></br>
            
                <% @items.each do |item| %>

                <div class="col-md-3">
             

                    <p><%= attachment_image_tag item, :image, :fill, 220,  220, format: 'jpeg' %></p>
                    <p><%= link_to item.name, item_path(item) %></p>

                    <p><%= (item.price * 1.1.round(2)).round %>円</p>
                     </div>
                <% end %>
            <div class="pull-right">
                <%= link_to "もっと見る>", items_path , class:"btn btn-default btn-lg active"%>
            </div>
        </div>
    </div>
</div>


